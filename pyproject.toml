[project]
name = "discord-bots"
version = "0.1.0"
description = "discord bot for mandy and jemmy to track things!"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "discord-py>=2.7.1",
    "pydantic>=2.13.4",
    "tinydb>=4.8.2",
]
